import { Storage } from "../../../internal/common/Storage";
import AuthInfo from "../AuthInfo";
import StoreCoreUserProfile from "./StoreCoreUserProfile";
declare function readFromStorage(storage: Storage): AuthInfo | undefined;
declare function writeToStorage(authInfo: AuthInfo, storage: Storage): void;
declare class StoreAuthInfo extends AuthInfo {
    readonly userProfile?: StoreCoreUserProfile | undefined;
    static readonly STORAGE_NAME: string;
    static decode(from: any): StoreAuthInfo;
    constructor(userId: string, deviceId: string, accessToken: string, refreshToken: string, loggedInProviderType: string, loggedInProviderName: string, userProfile?: StoreCoreUserProfile | undefined);
    encode(): object;
}
export { StoreAuthInfo, readFromStorage, writeToStorage };
